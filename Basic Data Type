Find the Runner-Up Score

if __name__ == '__main__':
    n = int(input())
    arr = list(map(int, input().split()))
    
    a=max(arr)
    b=arr.count(max(arr))
    
    for i in range(b):
        arr.remove(max(arr))
    print(max(arr))    
    
